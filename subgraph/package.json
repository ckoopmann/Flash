{
  "name": "flash",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ flash",
    "create-local": "graph create --node http://localhost:8020/ flash",
    "remove-local": "graph remove --node http://localhost:8020/ flash",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 flash",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "@radix-ui/react-scroll-area": "^1.0.5",
    "@radix-ui/react-separator": "^1.0.3"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
